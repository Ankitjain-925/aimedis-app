{
  "name": "database",
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "prisma": "latest",
    "glob": "^9.0.1"
  },
  "scripts": {
    "prisma-merge": "node ./merge.js",
    "db:generate": "prisma generate",
    "db:push": "node ./merge.js && prisma db push --skip-generate"
  },
  "main": "./index.js"
}
